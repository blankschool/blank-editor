PORT=8791
# Caminho do Python usado para scripts/canva-pdf-fonts.py — precisa ter fonttools, brotli e
# pymupdf instalados (python3 -m venv .venv && .venv/bin/pip install fonttools brotli pymupdf).
CANVA_IMPORT_PYTHON=.venv/bin/python3
# Compartilhado com PDF_IMPORT_SERVICE_SECRET do blank-editor/server — sem isto, /importar aceita
# qualquer chamada (ok em dev local, nunca em produção).
IMPORT_SECRET=
