# Copie para .env.docker se quiser exercitar login/storage de verdade no ambiente em container.
# Sem este arquivo o compose sobe normalmente: /api/v1/auth/* responde 501 e o resto do app
# funciona por chave Bearer. O .env.docker é ignorado pelo git — não commite credencial.

# SUPABASE_URL=https://sites-blank-editor-supabase.ickanz.easypanel.host
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# Importar PDF (microsserviço separado):
# PDF_IMPORT_SERVICE_URL=
# PDF_IMPORT_SERVICE_SECRET=
