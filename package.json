{
  "name": "blank-editor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "node --test src/*.test.ts",
    "import:figma": "node scripts/figma-import.mjs",
    "test:fidelity": "node tests/fidelity.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
